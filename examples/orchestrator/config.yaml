model:
  primary: mistral-7b-instruct-v0.3.Q4_K_M
  fallback_mode: disabled

inference:
  temperature: 0.4
  max_tokens: 4096
  tool_call_format: llama3

memory:
  max_history_turns: 20

# The orchestrator delegates by calling other pods exposed in ToolRegistry.
