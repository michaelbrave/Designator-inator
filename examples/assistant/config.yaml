model:
  primary: mistral-7b-instruct-v0.3.Q4_K_M
  fallback_mode: disabled

inference:
  temperature: 0.7
  max_tokens: 4096
  tool_call_format: llama3

memory:
  max_history_turns: 20

# API keys are never stored here.
# To enable cloud fallback, set the environment variable and change fallback_mode above:
#
#   providers:
#     anthropic:
#       api_key_env: ANTHROPIC_API_KEY
#     openai:
#       api_key_env: OPENAI_API_KEY
